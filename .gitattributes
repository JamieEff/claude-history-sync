# Normalise line endings across machines.
* text=auto

# Shell scripts MUST be LF or the shebang breaks on macOS/Linux.
*.sh text eol=lf

# PowerShell scripts: keep CRLF on Windows checkouts.
*.ps1 text eol=crlf
